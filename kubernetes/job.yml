apiVersion: batch/v1
kind: Job
metadata:
  name: db-backup-job
  namespace: bank-app
spec:
  template:
    spec:
      containers:
      - name: backup
        image: busybox
        command: ["sh", "-c", "echo 'Backing up DB...' && sleep 5"]
      restartPolicy: Never
  backoffLimit: 3
