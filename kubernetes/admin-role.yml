apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: bank-app
  name: admin-role
rules:
- apiGroups: [""]
  resources: ["pods", "services"]
  verbs: ["get", "list", "create", "delete", "update"]
